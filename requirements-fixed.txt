# Core voice processing dependencies
speechrecognition==3.10.0
pyttsx3==2.90
gtts==2.4.0
pygame==2.6.1

# Windows-specific voice improvements (if available)
pywin32>=306; sys_platform == "win32"
comtypes>=1.2.0; sys_platform == "win32"

# Basic dependencies (no external requirements)
# All other functionality is built-in to Python